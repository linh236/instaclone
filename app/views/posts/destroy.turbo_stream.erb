<%= turbo_stream.remove "post-#{@post.id}" do %>
    <%= render partial: "post", locals: {post: @post}%>
<% end %>

<%= turbo_stream.append "flash", partial: "shared/flash", locals: { msg_type: :notice, message: "This post is removed!" } %>
