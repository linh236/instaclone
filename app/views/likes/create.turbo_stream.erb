<%= turbo_stream.update "post-likes-count-#{@post.id}" do %>
  <%= pluralize(@post.likes.count, "like") %>
<% end %>

<%= turbo_stream.update "post-likes-#{@post.id}" do %>
  <%= button_to "", post_likes_path(@post), class:"post-actions-unlike", method: :delete%>
<% end %>