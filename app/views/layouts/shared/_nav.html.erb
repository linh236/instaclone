<div class="nav">
  <div class="container <%=user_signed_in? ? 'nav-sign-in' : 'nav-none'%>">
    <% if user_signed_in? %>
      <%= link_to "Home", root_path %>
      <%= link_to "Sign out", destroy_user_session_path, data: {turbo_method: :delete} %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path %>
    <% end %>
  </div>
</div>