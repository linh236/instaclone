<nav class="navbar navbar-expand-lg bg-light sticky-top">
  <div class="container">
  <%= link_to 'DN Church', root_path, class: "navbar-brand"%>
    <%# <a class="navbar-brand" href="#">Instaclone</a> %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mb-2 mb-lg-0 nav-tabs nav-justified w-100">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link #{request.path == root_path ? 'active' : ''}" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Lists", musics_path, class: "nav-link #{request.path == musics_path ? 'active' : ''}" %>
          </li>        
        <% end %>
      </ul>
      <div class="nav-item dropdown">
        <a class="nav-link" href="#" id="profile" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <img src="https://s3.eu-central-1.amazonaws.com/bootstrapbaymisc/blog/24_days_bootstrap/fox.jpg" width="40" height="40" class="rounded-circle">
        </a>
        <div class="dropdown-menu" aria-labelledby="profile">
          <% if user_signed_in? %>
            <a class="dropdown-item" href="#">Dashboard</a>
            <a class="dropdown-item" href="#">Edit Profile</a>
            <%= link_to "Sign out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "dropdown-item" %>
          <% end %>
        </div>
      </div>   
    </div>
  </div>
</nav>